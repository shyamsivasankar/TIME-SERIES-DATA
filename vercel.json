{
  "version": 2,
  "builds": [
    { 
      "src": "main.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "20mb",
        "cmd": ["pip", "install", "-r", "requirements.txt", "&&", "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "$PORT"]
      }
    }
  ]
}